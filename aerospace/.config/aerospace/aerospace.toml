# AeroSpace Tiling Window Manager Configuration
# https://nikitabobko.github.io/AeroSpace/
#
# Custom Keybindings:
#   Mirrors Omarchy/Hyprland's base-tier-vs-heavier-tier split (bare Super vs
#   Super+Shift) using meh/hyper in place of Super, since Cmd/Super alone
#   isn't usable for global bindings on macOS. Arrows below are produced by
#   a kanata layer, not plain keypresses — hold Space, then h/j/k/l (see
#   ~/.config/kanata). Digits are real physical keys on this keyboard, no
#   layer involved.
#
#   meh (ctrl-alt-shift), base tier:
#     Focus:      hold Space, h/j/k/l - directional focus
#     Workspace:  1-5 - switch to workspace 1-5
#     Fullscreen: f - toggle fullscreen
#     Layout:     [ - toggle accordion/tiles
#     Service:    ; - enter service mode (reload, reset layout, join windows)
#
#   hyper (ctrl-alt-shift-cmd), heavier tier:
#     Move:       hold Space, h/j/k/l - directional swap/move
#     Move+Go:    1-5 - move window to workspace 1-5 and follow
#
#   Floating/tiling toggle (meh-/), resize (meh-m/n, shrink/grow), and the
#   consolidated pop-free action (meh-Backspace) all live in Hammerspoon's
#   windows.lua — resize shells out to `aerospace resize smart` when tiled,
#   or resizes the frame directly when floating; see ~/.hammerspoon

after-startup-command = [
    'exec-and-forget sketchybar',
    'exec-and-forget $HOME/.config/borders/bordersrc'
]

# Notify Sketchybar about workspace changes
exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE'
]

# Notify Sketchybar about window focus changes. No mouse-follows-focus here:
# AeroSpace can't tell a click-driven focus change from a keybind-driven one,
# so window-lazy-center's "skip if already inside" guard warped the cursor on
# plain clicks too often to be worth it.
on-focus-changed = [
    'exec-and-forget sketchybar --trigger window_focus'
]

config-version = 2

# Keeps workspaces 1-5 alive when empty so sketchybar always lists all 5.
# Details: https://github.com/nikitabobko/AeroSpace/issues/999
persistent-workspaces = ["1", "2", "3", "4", "5"]

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 28

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# Possible values: (qwerty|dvorak|colemak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
    preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See:
#                 https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
    inner.horizontal = [{ monitor."Studio Display" = 48 }, 16]
    inner.vertical =   [{ monitor."Studio Display" = 48 }, 16]
    outer.left =       [{ monitor."Studio Display" = 48 }, 16]
    outer.bottom =     [{ monitor."Studio Display" = 48 }, 16]
    outer.top =        [{ monitor."Studio Display" = 86 }, 24]
    outer.right =      [{ monitor."Studio Display" = 48 }, 16]

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

    # All possible keys:
    # - Letters.        a, b, c, ..., z
    # - Numbers.        0, 1, 2, ..., 9
    # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
    # - F-keys.         f1, f2, ..., f20
    # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
    #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
    #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
    #                   sectionSign (ISO keyboards only, european keyboards only)
    # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
    #                   keypadMinus, keypadMultiply, keypadPlus
    # - Arrows.         left, down, up, right

    # All possible modifiers: cmd, alt, ctrl, shift

    # All possible commands: https://nikitabobko.github.io/AeroSpace/commands

    # See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
    # You can uncomment the following lines to open up terminal with alt + enter shortcut
    # (like in i3)
    # alt-enter = '''exec-and-forget osascript -e '
    # tell application "Terminal"
    #     do script
    #     activate
    # end tell'
    # '''

    # See: https://nikitabobko.github.io/AeroSpace/commands#layout
    # ctrl-alt-shift-slash = 'layout tiles horizontal vertical'
    # ctrl-alt-shift-comma = 'layout accordion horizontal vertical'
    ctrl-alt-shift-leftSquareBracket = 'layout accordion tiles'

    # Focus/swap below are keyed against genuine arrow keysyms (hold Space,
    # press h/j/k/l), not the plain letters — matches the mental model that
    # "arrow" always means holding Space, and matches Omarchy's own
    # SUPER+Arrow / SUPER+SHIFT+Arrow key choice.
    # See: https://nikitabobko.github.io/AeroSpace/commands#focus
    ctrl-alt-shift-left = 'focus left'
    ctrl-alt-shift-down = 'focus down'
    ctrl-alt-shift-up = 'focus up'
    ctrl-alt-shift-right = 'focus right'
    # See: https://nikitabobko.github.io/AeroSpace/commands#mode
    ctrl-alt-shift-semicolon = 'mode service'
    ctrl-alt-shift-f = 'fullscreen'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move
    ctrl-alt-shift-cmd-left = 'move left'
    ctrl-alt-shift-cmd-down = 'move down'
    ctrl-alt-shift-cmd-up = 'move up'
    ctrl-alt-shift-cmd-right = 'move right'

    # Workspace switch/move+follow below are keyed against the real number
    # keys (no kanata layer involved) rather than a/s/d/f/g or q/w/e/r/t —
    # matches Omarchy's own SUPER+num / SUPER+SHIFT+num key choice.
    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace
    ctrl-alt-shift-1 = 'workspace 1'
    ctrl-alt-shift-2 = 'workspace 2'
    ctrl-alt-shift-3 = 'workspace 3'
    ctrl-alt-shift-4 = 'workspace 4'
    ctrl-alt-shift-5 = 'workspace 5'

    # See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
    ctrl-alt-shift-cmd-1 = ['move-node-to-workspace 1', 'workspace 1']
    ctrl-alt-shift-cmd-2 = ['move-node-to-workspace 2', 'workspace 2']
    ctrl-alt-shift-cmd-3 = ['move-node-to-workspace 3', 'workspace 3']
    ctrl-alt-shift-cmd-4 = ['move-node-to-workspace 4', 'workspace 4']
    ctrl-alt-shift-cmd-5 = ['move-node-to-workspace 5', 'workspace 5']

    # See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
    # ctrl-alt-shift-tab = 'workspace-back-and-forth'
    # See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
    # ctrl-alt-shift-cmd-tab = 'move-workspace-to-monitor --wrap-around next'


# Window rules
# To find app-id (bundle identifier):
# - Regular apps: mdls -name kMDItemCFBundleIdentifier /Applications/AppName.app
# - Safari SSBs: /usr/libexec/PlistBuddy -c "Print CFBundleIdentifier" ~/Applications/AppName.app/Contents/Info.plist
#   (Safari SSBs use format: com.apple.Safari.WebApp.{UUID})

# Tile everything by default (AeroSpace's default behavior). Apps below are
# opted into floating instead.
# Toggle the current window manually with ctrl-alt-shift-cmd-p
[[on-window-detected]]
    if.app-id = 'com.apple.calculator'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.iCal'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.anthropic.claudefordesktop'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.finder'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.mail'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.MobileSMS'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.manytricks.Moom'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.Music'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'org.whispersystems.signal-desktop'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.culturedcode.ThingsMac'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.Safari.WebApp.4EF52CD3-780D-4265-8E9B-5EB7650B3196'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.Safari.WebApp.3D36C4CC-1ED4-4945-BB26-8A5373748889'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.Safari.WebApp.A140371B-6529-45EF-8F73-A9942E094D74'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.Safari.WebApp.ABA59A85-ADF9-4799-98A6-078299274FDE'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'us.zoom.xos'
    run = 'layout floating'

[[on-window-detected]]
    if.app-id = 'com.apple.weather'
    run = 'layout floating'

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
    esc = 'mode main'
    r = ['reload-config', 'mode main']
    x = ['flatten-workspace-tree', 'mode main'] # reset layout

    # sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
    #s = ['layout sticky tiling', 'mode main']

    h = ['join-with left', 'layout accordion', 'mode main']
    j = ['join-with left', 'mode main']
    k = ['join-with right', 'mode main']
    l = ['join-with right', 'layout accordion', 'mode main']
